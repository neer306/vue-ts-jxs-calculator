export enum MutationTypes {
  ADD_DIGIT       = 'addDigit',
  CANCEL          = 'cancel',
  APPLY_OPERATION = 'applyOperation',
  EQUAL           = 'equal',
  SET_LOADING     = 'setLoading',
}

export enum Actions {
  EQUAL = 'equal',
}
